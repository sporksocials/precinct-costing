node_modules
.next
out
.env
.env.local
.env*.local
*.tsbuildinfo
next-env.d.ts
# local visual-QA fixture + screenshots (client pricing data — never commit or deploy)
.demo
# every working file for this project (client sheets, source data, research, archives): client pricing, never commit
/files/
